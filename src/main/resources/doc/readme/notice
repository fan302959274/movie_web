1、日志引用说明:
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

protected final Logger logger = LogManager.getLogger(this.getClass());

2、权限配置
添加初始用户admin后期添加的所有用户初始密码都是000000